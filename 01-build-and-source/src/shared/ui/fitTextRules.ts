/**
 * `ui/fitText.ts`의 순수 쌍둥이 — 축소 하한만 담는다.
 *
 * 왜 갈랐나: `fitText.ts`는 `Text`(pixi)를 받으므로 node 테스트가 못 불러온다
 * (`navigator is not defined`). 그런데 "이 라벨이 칸에 들어가는가"는 폭 예산의
 * 산술이라 테스트가 물어야 하는 질문이고, 그 답에 하한이 필요하다
 * (`SHARED-API.md` §0-1).
 */

/**
 * 글자를 줄이는 하한 배율. 이보다 더 줄여야 들어가면 말줄임으로 넘어간다.
 *
 * 0.75인 이유: 도트 폰트라 축소가 반 픽셀을 만들고, 그보다 작아지면 24px
 * 글자가 읽히지 않는다. 값을 옮기려면 캡처로 판정해라 — 배율은 코드에서
 * 안 보인다.
 */
export const FIT_MIN_SCALE = 0.75;
